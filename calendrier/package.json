{
  "name": "calendrier-api",
  "version": "1.0.0",
  "private": true,
  "description": "API calendrier avec présentation sur 7 jours (créneaux 18:00 / 18:30, 5 lieux).",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test",
    "deploy": "wrangler deploy",
    "dev:cloudflare": "wrangler dev",
    "cloudflare:init": "node outils/cloudflare-init.js"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "UNLICENSED"
}
